# ADR 0001：先采用无构建静态网页

## 状态

已采用

## 背景

当前目标是先把公众号排版参数和预设交互跑通；参考文章尚未提供，暂时不需要后端抓取或账号能力。

## 决策

网页使用原生 HTML、CSS 和 JavaScript，参数用 JSON 表示，并提供导入/导出。

## 原因

- 双击即可打开，方便产品经理快速试用；
- 参数结构未来可以直接接到提取 API；
- 不引入构建链，降低修改成本；
- 富文本复制可在浏览器中直接验证。

## 后续触发条件

当需要真实公众号链接抓取、账号保存、多人共享或模板版本管理时，再评估接入 Next.js/API。
